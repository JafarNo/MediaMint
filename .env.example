
# Deployed backend URL - REQUIRED for production
EXPO_PUBLIC_API_BASE_URL=https://aya.nuthre.com

# API request timeout in milliseconds (default: 30000 = 30 seconds)
EXPO_PUBLIC_API_TIMEOUT=30000

# Video generation timeout in milliseconds (default: 90000 = 90 seconds)
# Video generation takes longer, so this has a higher timeout
EXPO_PUBLIC_VIDEO_TIMEOUT=90000
